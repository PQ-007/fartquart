---
title:                                    # Book title
date: 2026-01-01                          # YYYY-MM-DD — when you finished / reviewed it
label: book-review                        # Keep as "book-review" → renders a Goodreads-style page at /blog/[slug]
author:                                   # Author's name (shown in the header)
rating: 4                                 # 1–5, decimals ok (e.g. 4.5) — rendered as stars
genre:                                    # e.g. Software Engineering (shown as a pill)
pages:                                    # Page count, a number (shown as "431p")
description:                              # One-line take — shown on cards & link previews
tags:                                     # One per line; each becomes a node in the /tags graph (case-sensitive)
  - 
cover: resources/images/folder/cover.jpg  # 2:3 book cover; put it in content/resources/images/; delete if none
draft: true                               # true = hidden while writing; set false to publish
---

> The cover, rating, author, genre, and page count render automatically in
> the header above. Write the actual review below. For a chaptered, note-by-note
> read use a `book-note/` folder instead. Delete this note.

Content here.
